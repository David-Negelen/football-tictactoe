# Copy to .env on the server (never commit the real .env) and fill in real values:
#   UMAMI_DB_PASSWORD: any long random string, e.g. `openssl rand -hex 32`
#   UMAMI_APP_SECRET:  any long random string, e.g. `openssl rand -hex 32`
UMAMI_DB_PASSWORD=
UMAMI_APP_SECRET=
